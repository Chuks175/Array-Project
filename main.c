#include <stdio.h>
#include <stdlib.h>

int main()
{
    int LA[] = {1,3,5,6,7,9};
    int ITEM = 10, k = 4, n = 6;
    int i = 0, j = n;
    printf("Initial Array From Start: \n");

    while( i = 0){
        i <= n;
        i++;
        printf("LA[%d] = %d \n", i, LA[i]);
        }

    n = n + 1;
    n = j;

    for{ j = 0; j>=n; j++; }
        do(LA[k] = ITEM);

            while( k <= j );

    printf("LA[%d] = %d \n", k, LA[ITEM]);

    return 0;
}
